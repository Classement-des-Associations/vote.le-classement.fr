@set('color', '')
@set('className',
String(`rounded-lg hover:border-primary-variation-1 focus:border-primary-variation-1
hover:bg-primary-variation-1 focus:bg-primary-variation-1 px-4 py-3 text-white text-sm leading-4 font-semibold
focus:outline-none transition ease-in`))

@if(accentPurple)
@set('color', 'border-accent-purple bg-accent-purple')
@else
@set('color', 'border-primary-base bg-primary-base')
@endif

{{{ await $slots.main({ className, color }) }}}
